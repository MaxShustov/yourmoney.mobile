<Page
    x:Class="YourMoney.UWP.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourMoney.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding LoginViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox PlaceholderText="User Name" Text="{Binding UserName, Mode=TwoWay}" VerticalAlignment="Center" Margin="10,0,10,0"/>
        <TextBox PlaceholderText="Password" Text="{Binding Password, Mode=TwoWay}" VerticalAlignment="Center" Margin="10,80,10,0"/>
        <Button Command="{Binding LoginCommand}" HorizontalAlignment="Stretch" Margin="10,160,10,0">Login</Button>
    </Grid>
</Page>
