<Page
    x:Class="YourMoney.UWP.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourMoney.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding LoginViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center">
            <TextBox PlaceholderText="User Name" Text="{Binding UserName, Mode=TwoWay}" VerticalAlignment="Center" Margin="10,0,10,10"/>
            <TextBox PlaceholderText="Password" Text="{Binding Password, Mode=TwoWay}" VerticalAlignment="Center" Margin="10,0,10,10"/>
            <Button Command="{Binding LoginCommand}" HorizontalAlignment="Stretch" Margin="10,0,10,0">Login</Button>
            <TextBlock HorizontalAlignment="Center" Margin="0,5,0,0">OR</TextBlock>
            <Button Command="{Binding RegisterCommand}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="10,10,10,0">Register</Button>
        </StackPanel>
    </Grid>
</Page>
