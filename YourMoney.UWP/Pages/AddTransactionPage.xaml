<Page
    x:Class="YourMoney.UWP.Pages.AddTransactionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourMoney.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding AddIncomeTransactionViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center">
            <TextBox Text="{Binding Description, Mode=TwoWay}" PlaceholderText="Description" Margin="10,0,10,5"/>
            <TextBox Text="{Binding SelectedCategory, Mode=TwoWay}" PlaceholderText="Category" Margin="10,5"/>
            <TextBox Text="{Binding Value, Mode=TwoWay}" PlaceholderText="Value" Margin="10,5"/>
            <Button Content="Add Transaction" Command="{Binding AddTransactionCommand}" HorizontalAlignment="Stretch" Margin="10,5,10,0"/>
        </StackPanel>
    </Grid>
</Page>
