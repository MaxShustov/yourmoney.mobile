<Page
    x:Class="YourMoney.UWP.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourMoney.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding HomeViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Your current balance: " Margin="20,20,5,0" HorizontalAlignment="Right"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentBalance}" Margin="5,20,0,0" HorizontalAlignment="Left"/>
        <Button Grid.Row="1" Grid.Column="0" Content="Income" Margin="5" HorizontalAlignment="Stretch"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Outcome" Margin="5" HorizontalAlignment="Stretch"/>
        <ListView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            
        </ListView>
    </Grid>
</Page>
